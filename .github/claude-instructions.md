# Claude Instructions

Please read and adhere strictly to the global architectural context, adapter patterns, and module definitions specified in `./PROJECT_CONTEXT.md`.

When working inside the `CAPPY/` directory, respect the canonical `HealthData` normalization flow and keep the `cappy_simulator.py` execution layer fully working in simulation mode.